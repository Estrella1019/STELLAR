# STELLAR 实验配置总结

## ✅ 已完成的工作

### 1. 清理旧数据
```bash
✅ 已清空 results/ 和 wandb/ 目录
```

### 2. 创建三组实验脚本

按照你提供的图片配置：

| 实验 | 算法 | 种群大小 | 代数 | 脚本文件 |
|------|------|---------|------|----------|
| RANDOM | rs | 2000 | 1 | `run_experiment_1_random.sh` |
| T-wise | gs | 2000 | 1 | `run_experiment_2_twise.sh` |
| STELLAR | nsga2 | 20 | 100 | `run_experiment_3_stellar.sh` |

### 3. 修复 Failure 为 0 的问题

**问题原因**:
- Fitness 评分解析失败 → 总是返回 0.0
- 导致所有测试都被判定为失败或都被判定为安全

**解决方案**:
- ✅ 增强 Fitness 评分解析（支持多种格式）
- ✅ 增加关键词启发式判断
- ✅ 分析响应内容识别拒答
- ✅ 基于 Fitness 阈值（0.7）判定 Critical

### 4. WandB 集成

实验会自动上传到 WandB:
- 项目名: `stellar-reproduction`
- 按日期分组
- 包含所有关键指标

### 5. 结果分析脚本

`analyze_results.py` 会自动生成:
- ✅ 失败发现曲线对比图
- ✅ 总失败数对比柱状图
- ✅ 失败率对比图
- ✅ 文本报告（包含改进倍数）

---

## 🚀 立即开始实验

### 一键运行（推荐）

```bash
cd /Users/panjiaying/Documents/Projects/STELLAR
./run_all_experiments.sh
```

这会自动运行所有三组实验（约 6 小时），然后生成对比图表。

### 或分别运行

```bash
# 实验 1: RANDOM
./run_experiment_1_random.sh

# 实验 2: T-wise
./run_experiment_2_twise.sh

# 实验 3: STELLAR
./run_experiment_3_stellar.sh

# 分析结果
python analyze_results.py
```

---

## 📊 实验结果查看

### 本地文件

所有结果在 `results/日期/` 目录:

```
results/01-02-2026/
├── random/
│   ├── failures_over_time.csv   # 失败数时间曲线
│   ├── archive.csv               # 所有测试用例
│   └── critical_cases.csv        # 攻击成功的用例
├── twise/
│   └── ...
├── stellar/
│   └── ...
└── analysis/                     # 自动生成
    ├── failures_over_time.png    # 对比曲线图
    ├── total_failures.png        # 总失败数对比
    ├── failure_rate.png          # 失败率对比
    └── report.txt                # 文本报告
```

### WandB 在线查看

1. 访问 https://wandb.ai
2. 进入项目 `stellar-reproduction`
3. 查看三组实验的对比仪表板

关键指标:
- `failures_count` - 发现的失败数
- `failure_rate` - 失败率
- `critical_ratio` - Critical 比率

---

## 🎯 预期结果

根据论文，STELLAR 应该比基线方法发现 **2-4倍** 更多的失败。

示例:
- RANDOM: ~100 failures
- T-wise: ~150 failures
- **STELLAR: ~300 failures** ✨

---

## ⚠️ 重要提示

### 验证 Judge 工作正常

运行实验前，先验证 Judge 能检测到失败:

```bash
python test_judge_detection.py
```

**预期输出**:
- 检测到失败数: 3-5 个（不是 0 也不是 7）
- 失败率: 40%-70%

如果结果异常:
- **0% 失败率** → Judge 太严格，降低阈值
- **100% 失败率** → Judge 太宽松，提高阈值

### 监控实验进度

```bash
# 实时查看日志
tail -f log.txt

# 查看当前失败数
tail results/$(date +%d-%m-%Y)/random/failures_over_time.csv
```

---

## 📚 参考文档

- `实验执行指南.md` - 详细执行步骤和故障排查
- `配置完成总结.md` - 技术细节和配置说明
- `快速开始.md` - 快速参考卡片

---

## ✅ 准备清单

- [x] 清理旧数据
- [x] Ollama 服务运行
- [x] 模型已下载（deepseek-r1:7b, dolphin3:latest）
- [x] 配置文件正确（.env）
- [x] Judge 评估逻辑修复
- [x] 实验脚本准备完成
- [x] 分析脚本准备完成
- [ ] 验证 Judge 工作 (`python test_judge_detection.py`)
- [ ] 开始实验 (`./run_all_experiments.sh`)

---

**一切就绪！现在可以运行实验了。** 🎉
